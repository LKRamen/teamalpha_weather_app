User can enter a location (city, state)
App displays forecast for that location over the next 5 days
User can reset all data and start over
You can write this as a web app or send a recording of a quick demo of the app being used.

Input city and state into Bing Maps Locations API to get the geocoding coords
Input coords into National Weather Service API and output the forecasts over next 5 days